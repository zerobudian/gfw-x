# Binaries
/gfwx
/dist/

# Repo-root release archives (should never be committed into the tree)
/gfw-x-*.zip

# Web build deps & output
# NOTE: internal/api/dist/ is intentionally TRACKED. The dashboard bundle is
# embedded via `//go:embed dist/*`, so committing it keeps a fresh clone able
# to `go build` without a Node toolchain. Regenerate with `make web`.
web/node_modules/
web/tsconfig.tsbuildinfo

# Runtime data / logs
data/

# Export output
/export/

# Go
*.test
*.out
coverage.*

# Editor / OS
.vscode/
.idea/
.DS_Store

# IDE / preview share packages (not part of the 1.1 deliverable)
.trae-html-share-packages/